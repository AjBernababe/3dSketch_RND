import * as THREE from 'three'

export const wireFrameContainer = new Map(); //Container of borders for onHover
export const connectionGroup = new THREE.Group(); //Group container of the whole connection