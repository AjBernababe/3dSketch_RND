import * as THREE from 'three'

export const meshBorderContainer = new Map(); //Container of borders for hoverEvents
export const objectGroup = new THREE.Group(); //Container of object and it's border
export const connectionGroup = new THREE.Group(); //Container of the whole connection